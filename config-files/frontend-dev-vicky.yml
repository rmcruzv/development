security.sessions: NEVER
logging.level.: WARN
server.port: 8083
spring.redis.host: localhost
eureka:
  client:
    serviceUrl:
      defaultZone: https://localhost:8761/eureka/
  instance:
    hostname: localhost
